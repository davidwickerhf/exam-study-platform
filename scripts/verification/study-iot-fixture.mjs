// Synthetic course evidence for the mechanisms in the user's teaching audit.
// Timings are explicit teaching inputs, never real measurements or exam questions.
export const evaluationCourse={courseCode:'EVALIOT',courseName:'IoT scheduling mechanisms',academicYear:'2026-2027',period:'1'}
export const evaluationSources=[{key:'current',title:'Current IoT mechanisms and scope',kind:'canvas',academicYear:'2026-2027',period:'1'}]
export const evaluationChunks=[{id:'iot-current',sourceKey:'current',page:1,text:`This module concerns polling, interrupts, cooperative scheduling, blocking and notification, and priority-preemptive scheduling on one processor. DMA and multicore synchronization are explicitly excluded from this assessment. All numerical scenarios below are illustrative teaching inputs; ignore context-switch and other overhead unless specified.
Polling reads an input at scheduled instants. It observes the value only at those checks; it cannot reconstruct an intervening pulse that has ended. Checks at 0, 100, and 200 ms detect a press starting at 30 ms at the 100 ms check (70 ms delay), and a release at 170 ms at the 200 ms check (30 ms delay). A pulse from 30 to 70 ms is missed by those checks because the value is inactive at both 0 and 100 ms.
An interrupt suspends main execution, preserves the execution context, runs an interrupt service routine (ISR), and returns to schedulable work. If main work needs 100 ms of CPU time, starts at 0, and is interrupted once at 30 ms for an 8 ms ISR, it finishes at 108 ms under the assumptions above. The ISR occupies 30 through 38 ms and does not perform any of those 100 ms of main work. An action explicitly performed inside the ISR happens there; work deferred to a task still depends on that task being scheduled. Keep handlers short.
A cooperative scheduler cannot preempt a running task merely because another task has higher priority. A logger that keeps the CPU for 20 ms without yielding or blocking delays an urgent motor-stop task by that remaining time. Raising the motor task's priority alone does not make the logger yield. An interrupt may still run in this system, but that does not turn deferred cooperative task scheduling into preemption.
A Blocked task waits for its stated kernel object or timeout; a Ready task is eligible for CPU time; a Running task has the CPU. Writing an ordinary shared variable does not notify the RTOS that a task blocked on a task notification should wake. Sending the relevant notification can release that notification wait. A timeout or another wake-up mechanism must be stated separately, never assumed. A polling task or an ISR explicitly reading a flag is different from a task blocked on a kernel notification.
On a single-core priority-preemptive system, after an ISR returns the highest-priority Ready task runs. Becoming Ready is not the same as becoming Running. In the illustrative integrated case, a button task is blocked on a notification, and a higher-priority logger has 70 ms of CPU work LEFT when the interrupt starts at time 30 ms. An 8 ms ISR sends the notification and returns at 38 ms. The button task becomes Ready when notified, but the logger continues until 108 ms; only then can the button task run. If the button task instead has higher priority than the logger, it can run at 38 ms. If the ISR only writes an ordinary flag and never notifies, the task remains blocked on that notification in the absence of a stated timeout or other wake-up. The 70 ms is remaining work at time 30, not the logger's original total.`}]
